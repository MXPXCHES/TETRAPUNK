"use client";

import BaseToolbarButton from "../base";
import icons from "@/assets/icons";

function DonateButton() {
  return (
    <BaseToolbarButton
      name={"Donar"}
      iconUrl={icons.heart}
      onClick={() => {}}
    />
  );
}

export default DonateButton;
