"use client";

import BaseToolbarButton from "../base";
import icons from "@/assets/icons";

function StoreButton() {
  return (
    <BaseToolbarButton
      name={"Tienda"}
      iconUrl={icons.inventory}
      onClick={() => {}}
    />
  );
}

export default StoreButton;
