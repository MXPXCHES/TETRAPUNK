import BaseSettingsButton from "../base";
import icons from "@/assets/icons";

function ZoomInButton() {
  return <BaseSettingsButton name="Acercarse" iconUrl={icons.zoomIn.src} />;
}

export default ZoomInButton;
