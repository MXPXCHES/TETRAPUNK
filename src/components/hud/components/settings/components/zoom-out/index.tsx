import BaseSettingsButton from "../base";
import icons from "@/assets/icons";

function ZoomOutButton() {
  return <BaseSettingsButton name="Alejarse" iconUrl={icons.zoomOut.src} />;
}

export default ZoomOutButton;
