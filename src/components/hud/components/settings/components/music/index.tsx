import BaseSettingsButton from "../base";
import icons from "@/assets/icons";

function MusicButton() {
  return <BaseSettingsButton name="Música (ON)" iconUrl={icons.music.src} />;
}

export default MusicButton;
